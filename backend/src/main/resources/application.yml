server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reservasdb?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
